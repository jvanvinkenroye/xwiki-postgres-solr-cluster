# XWiki Cluster Environment Variables
# Copy this file to .env and customize as needed

# XWiki Version
XWIKI_VERSION=stable-postgres-tomcat

# Load Balancer Port (user access point)
XWIKI_PORT=8080

# Nginx Status Port
NGINX_STATUS_PORT=8081

# PostgreSQL Configuration
POSTGRES_ROOT_PASSWORD=changeme_root_password
POSTGRES_USER=xwiki
POSTGRES_PASSWORD=changeme_xwiki_password
POSTGRES_DB=xwiki

# Solr Configuration
# Increase for larger wikis or high query volume
SOLR_HEAP=2g

# Number of XWiki nodes (2 or 3 recommended)
# To disable node3, comment it out in docker-compose-cluster.yml
