# XWiki Docker Compose Environment Variables
# Copy this file to .env and customize as needed

# XWiki Version
# Options: stable-postgres-tomcat, lts-postgres-tomcat, or specific version like 17-postgres-tomcat
XWIKI_VERSION=stable-postgres-tomcat

# Port on which XWiki will be accessible on the host
XWIKI_PORT=8080

# PostgreSQL Configuration
POSTGRES_ROOT_PASSWORD=changeme_root_password
POSTGRES_USER=xwiki
POSTGRES_PASSWORD=changeme_xwiki_password
POSTGRES_DB=xwiki

# Solr Configuration
# Heap size for Solr (adjust based on your wiki size and available memory)
# Recommended: 512m for small wikis, 1g for medium, 2g+ for large wikis
SOLR_HEAP=512m
